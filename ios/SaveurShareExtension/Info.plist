<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Saveur</string>
	<key>CFBundleName</key>
	<string>SaveurShareExtension</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Broad, not per-file-type, to mirror MyDocuments.tsx's own
			     upload picker (pdf/doc/docx/plainText/images) plus common
			     file/URL shares from Mail/Files/a browser -- product ask
			     was "share files ... it will go directly to the document
			     section", not a narrower single-file-type integration.
			     NSExtensionActivationSupportsFileWithMaxCount and
			     NSExtensionActivationSupportsWebURLWithMaxCount together
			     cover "a file from Files/Mail" and "a link from Safari/
			     another app" respectively -- a shared web link is saved
			     as a small .webloc-style text/url file into the same
			     shared container as a real file share, so
			     ShareIntentModule.swift's read path doesn't need two
			     different code paths for "real file" vs. "link". -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsFileWithMaxCount</key>
				<integer>10</integer>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>10</integer>
			</dict>
		</dict>
		<key>NSExtensionMainStoryboard</key>
		<string>MainInterface</string>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
	</dict>
</dict>
</plist>
