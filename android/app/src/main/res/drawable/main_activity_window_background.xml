<?xml version="1.0" encoding="utf-8"?>
<!-- MainActivity's windowBackground — brand blue (@color/splash_background)
     with the Saveur logo centered at a fixed, known size we fully control.
     Deliberately NOT
     using the system SplashScreen API's windowSplashScreenAnimatedIcon for
     this logo (see Theme.App.SplashScreen in styles.xml, which now sets no
     icon at all) — that mechanism was tried twice with well-padded assets
     and still rendered the logo huge/cropped on-device, seemingly ignoring
     the source PNG's own transparent padding. A plain windowBackground
     drawable has no such platform-side auto-sizing: this <item>'s
     width/height directly control the on-screen box.

     Product request ("I want the splash screen to be like [reference]...
     The saveur logo and the app name will be on the same horizontal
     level") — was the solo "S" mark alone (@mipmap/ic_launcher_foreground,
     the app icon's own foreground layer). This Android launch background is
     a static drawable (no runtime text layout the way the iOS storyboards'
     Auto Layout row can do — see LaunchScreen.storyboard/BootSplash.
     storyboard's own matching comments for that side), so there's no way to
     lay a live text label out next to the mark here; @drawable/
     splash_logo_wordmark is a single pre-rendered PNG (drawable-mdpi/
     -hdpi/-xhdpi/-xxhdpi/-xxxhdpi, generated from this same logo mark +
     the app's own MontserratAlternates-Black wordmark font, matching the
     iOS storyboards' lockup) with the mark and "Saveur" already composited
     side by side. Box below sized to that asset's own real 402x100dp
     aspect ratio (was a 248x248dp square for the old solo glyph) so the
     bitmap renders at its native proportions without being stretched or
     letterboxed by a mismatched box shape. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <!-- BUG FIX, ROUND 3 (real-device report: STILL huge/overflowing after
         round 1 (sized item + android:drawable) AND round 2 (unsized item +
         android:drawable + gravity=center) — meaning the common thread
         across both failed rounds wasn't sizing at all, it was using
         android:drawable directly on the <item>. A plain
         <item android:drawable="@drawable/x"/> used as (part of) a
         windowBackground has no intrinsic size of its own to gravity
         against — a window background is stretched to fill the full window
         bounds, and item-level android:gravity is only meaningful when
         paired with an explicit android:width/height (round 1's approach,
         which apparently didn't take effect the way expected either) or
         when the item's content is a <bitmap> child instead of a direct
         drawable reference. <bitmap> is a distinct drawable type from a
         plain PNG/drawable reference: BitmapDrawable implements its OWN
         gravity-based draw() logic that positions the bitmap at its
         intrinsic size within whatever bounds it's given, WITHOUT
         stretching to fill — unlike a bare drawable reference. Nesting the
         PNG in a <bitmap android:gravity="center"> inside an otherwise
         unsized <item> is the standard, documented pattern for exactly this
         "background color + centered logo at native size" case, and is
         what actually gets the density-bucketed asset (already verified
         correctly proportioned per bucket — mdpi 402x100px, xhdpi
         804x200px, xxxhdpi 1607x400px, etc) to render at its real size
         instead of stretched edge-to-edge. -->
    <item>
        <bitmap
            android:src="@drawable/splash_logo_wordmark"
            android:gravity="center" />
    </item>
</layer-list>
